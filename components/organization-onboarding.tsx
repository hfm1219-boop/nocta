"use client";
import{FormEvent,useState}from"react";import{useRouter}from"next/navigation";import{BUSINESS_TYPES,PRINCIPAL_ROLE_LABELS,type BusinessType,type PrincipalRole}from"@/lib/auth/roles";
const LABELS:Record<BusinessType,string>={manufacturer:"Fabricante",importer:"Importador",distributor:"Distribuidor",brand_owner:"Dueño de marca",representative:"Representante",mixed:"Mixto"};
export function OrganizationOnboarding(){const router=useRouter();const[open,setOpen]=useState(false);const[context,setContext]=useState<Extract<PrincipalRole,"establishment"|"promoter"|"brand_distributor">>("brand_distributor");const[businessType,setBusinessType]=useState<BusinessType>("distributor");const[busy,setBusy]=useState(false);const[message,setMessage]=useState("");async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);setMessage("");const form=new FormData(e.currentTarget);const response=await fetch("/api/organizations",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({action:"create",name:form.get("name"),context,businessType:context==="brand_distributor"?businessType:undefined})});const body=await response.json();if(!response.ok){setBusy(false);return setMessage(body.error??"No fue posible crear la organización")};const organizationId=body.data as string;const activation=await fetch("/api/auth/context",{method:"PATCH",headers:{"Content-Type":"application/json"},body:JSON.stringify({organizationId,role:context})});setBusy(false);if(!activation.ok){const detail=await activation.json();return setMessage(detail.error??"Organización creada; no fue posible activarla")};router.replace(context==="brand_distributor"?"/marca":context==="promoter"?"/promotor":"/admin");router.refresh()}
return <section className="border-t border-line pt-5"><div className="flex items-center justify-between gap-4"><div><h2 className="font-bold">Nueva organización empresarial</h2><p className="text-sm text-muted mt-1">Crea un contexto de establecimiento, promotor o marca con esta misma cuenta.</p></div><button onClick={()=>setOpen(!open)} className="rounded-xl border border-neon2/40 px-4 py-2 text-sm text-neon2">{open?"Cerrar":"Crear"}</button></div>{open&&<form onSubmit={submit} className="mt-5 space-y-3"><label className="block text-xs text-muted">Tipo de organización<select value={context} onChange={e=>setContext(e.target.value as typeof context)} className="entrada"><option value="brand_distributor">Marca / Distribuidor</option><option value="promoter">Promotor</option><option value="establishment">Establecimiento</option></select></label><label className="block text-xs text-muted">Nombre<input name="name" required minLength={2} className="entrada" placeholder="Ej. Dismel"/></label>{context==="brand_distributor"&&<label className="block text-xs text-muted">Tipo empresarial<select value={businessType} onChange={e=>setBusinessType(e.target.value as BusinessType)} className="entrada">{BUSINESS_TYPES.map(type=><option key={type} value={type}>{LABELS[type]}</option>)}</select></label>}<button disabled={busy} className="btn-neon rounded-xl p-3 w-full disabled:opacity-50">{busy?"Creando…":`Crear ${PRINCIPAL_ROLE_LABELS[context]}`}</button>{message&&<p className="text-sm text-danger">{message}</p>}</form>}</section>}
