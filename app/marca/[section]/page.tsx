import {BrandConsole} from "../brand-console";export default async function BrandSection({params}:{params:Promise<{section:string}>}){const{section}=await params;return <BrandConsole section={section}/>}
