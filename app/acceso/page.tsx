"use client";

import { FormEvent, useEffect, useEffectEvent, useId, useRef, useState } from "react";
import Link from "next/link";
import { EVENTOS, eventoPorId } from "@/lib/discovery";
import { validarEntrada, useEntradas } from "@/lib/tickets";
import { validarReserva, useReservas } from "@/lib/reservations";
import { validarInvitado, useListasPromotor } from "@/lib/guest-lists";
import { Logo } from "@/components/ui";
import { useEventosPromotor } from "@/lib/promoter-events";

type Resultado = { estado:"aceptada"|"usada"|"invalida"; titulo:string; nombre?:string; detalle?:string };
type Hallazgo = { id:string; tipo:"entrada"|"reserva"|"lista"; codigo:string; nombre:string; detalle:string; eventoId:string; habilitado:boolean };

export default function ControlAcceso(){
  const reactId=useId();const elementId=`lector-entrada-${reactId.replace(/:/g,"")}`;const entradas=useEntradas();const reservas=useReservas();const listas=useListasPromotor();const planes=useEventosPromotor().filter(e=>e.estado!=="borrador");
  const [codigo,setCodigo]=useState("");const [busqueda,setBusqueda]=useState("");const [eventoId,setEventoId]=useState("");const [resultado,setResultado]=useState<Resultado|null>(null);const [errorCamara,setErrorCamara]=useState("");const bloqueado=useRef(false);
  const [beneficioPendiente,setBeneficioPendiente]=useState("");
  const eventoNombre=(id:string)=>eventoPorId(id)?.nombre??planes.find(e=>e.id===id)?.nombre??listas.find(l=>l.eventoId===id)?.eventoNombre??"Evento";
  async function comprobar(valor:string){
    if(bloqueado.current)return;bloqueado.current=true;const limpio=valor.trim();const codigoBase=limpio.replace(/^nocta:(entrada|reserva|lista):/,"");const entrada=entradas.find(e=>e.codigo===codigoBase);const reserva=reservas.find(r=>r.codigo===codigoBase);const lista=listas.find(l=>l.invitados.some(i=>i.codigo===codigoBase));const invitado=lista?.invitados.find(i=>i.codigo===codigoBase);
    if(/^[A-HJ-NP-Z2-9]{4}(?:-[A-HJ-NP-Z2-9]{4}){2}$/i.test(limpio)){try{const respuesta=await fetch("/api/access/validate-reward",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({code:limpio,action:"check"})});const remoto=await respuesta.json() as {valid?:boolean;reason?:string;reward_name?:string;participant_name?:string;venue_name?:string;error?:string};if(!respuesta.ok)throw new Error(remoto.error??"No fue posible validar el beneficio");setBeneficioPendiente(remoto.valid?limpio:"");setResultado({estado:remoto.valid?"aceptada":remoto.reason==="delivered"?"usada":"invalida",titulo:remoto.valid?"BENEFICIO VÁLIDO · CONFIRMA PARA APLICAR":remoto.reason==="delivered"?"BENEFICIO YA UTILIZADO":remoto.reason==="expired"||remoto.reason==="cancelled"?"BENEFICIO VENCIDO":"CÓDIGO INVÁLIDO",nombre:remoto.participant_name,detalle:remoto.reward_name&&remoto.venue_name?`${remoto.reward_name} · ${remoto.venue_name}`:remoto.reward_name});}catch(error){setBeneficioPendiente("");setResultado({estado:"invalida",titulo:"NO FUE POSIBLE VALIDAR",detalle:error instanceof Error?error.message:"Error de validación"});}window.setTimeout(()=>{bloqueado.current=false;},1200);return;}
    if(limpio.startsWith("nocta:entrada:")||entrada){try{const respuesta=await fetch("/api/access/validate",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({token:codigoBase})});const remoto=await respuesta.json() as {result?:"accepted"|"used"|"invalid";holder_name?:string;type_name?:string;event_key?:string;error?:string};if(!respuesta.ok)throw new Error(remoto.error??"No fue posible validar la entrada");if(eventoId&&remoto.event_key&&remoto.event_key!==eventoId){setResultado({estado:"invalida",titulo:"EVENTO INCORRECTO",nombre:remoto.holder_name,detalle:`Esta credencial pertenece a ${eventoNombre(remoto.event_key)}.`});}else{if(remoto.result==="accepted"&&entrada)validarEntrada(limpio);setResultado({estado:remoto.result==="accepted"?"aceptada":remoto.result==="used"?"usada":"invalida",titulo:remoto.result==="accepted"?"ACCESO PERMITIDO":remoto.result==="used"?"ENTRADA YA USADA":"ENTRADA INVÁLIDA",nombre:remoto.holder_name,detalle:remoto.type_name?`Entrada · ${remoto.type_name}`:undefined});}}catch(error){setResultado({estado:"invalida",titulo:"NO FUE POSIBLE VALIDAR",detalle:error instanceof Error?error.message:"Error de validación"});}window.setTimeout(()=>{bloqueado.current=false;},1200);return;}
    if(limpio.startsWith("nocta:reserva:")||reserva){try{const respuesta=await fetch("/api/access/validate-reservation",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({token:codigoBase})});const remoto=await respuesta.json() as {result?:"accepted"|"used"|"pending"|"invalid";customer_name?:string;zone_name?:string;party_size?:number;event_key?:string;error?:string};if(!respuesta.ok)throw new Error(remoto.error??"No fue posible validar la reserva");if(eventoId&&remoto.event_key&&remoto.event_key!==eventoId){setResultado({estado:"invalida",titulo:"EVENTO INCORRECTO",nombre:remoto.customer_name,detalle:`Esta credencial pertenece a ${eventoNombre(remoto.event_key)}.`});}else{if(remoto.result==="accepted"&&reserva)validarReserva(limpio);setResultado({estado:remoto.result==="accepted"?"aceptada":remoto.result==="used"?"usada":"invalida",titulo:remoto.result==="accepted"?"RESERVA RECIBIDA":remoto.result==="used"?"RESERVA YA UTILIZADA":remoto.result==="pending"?"RESERVA PENDIENTE":"RESERVA INVÁLIDA",nombre:remoto.customer_name,detalle:remoto.zone_name?`${remoto.zone_name} · ${remoto.party_size??0} personas`:undefined});}}catch(error){setResultado({estado:"invalida",titulo:"NO FUE POSIBLE VALIDAR",detalle:error instanceof Error?error.message:"Error de validación"});}window.setTimeout(()=>{bloqueado.current=false;},1200);return;}
    if(limpio.startsWith("nocta:lista:")||invitado){try{const respuesta=await fetch("/api/access/validate-guest",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({token:codigoBase})});const remoto=await respuesta.json() as {result?:"accepted"|"used"|"invalid";guest_name?:string;companions?:number;entered?:number;event_key?:string;list_name?:string;error?:string};if(!respuesta.ok)throw new Error(remoto.error??"No fue posible validar la invitación");if(eventoId&&remoto.event_key&&remoto.event_key!==eventoId){setResultado({estado:"invalida",titulo:"EVENTO INCORRECTO",nombre:remoto.guest_name,detalle:`Esta invitación pertenece a ${eventoNombre(remoto.event_key)}.`});}else{if(remoto.result==="accepted"&&invitado)validarInvitado(limpio);setResultado({estado:remoto.result==="accepted"?"aceptada":remoto.result==="used"?"usada":"invalida",titulo:remoto.result==="accepted"?"GRUPO EN LISTA INGRESADO":remoto.result==="used"?"INVITACIÓN YA UTILIZADA":"INVITACIÓN INVÁLIDA",nombre:remoto.guest_name,detalle:remoto.list_name?`${remoto.list_name} · ${remoto.entered??0} ingresados`:undefined});}}catch(error){setResultado({estado:"invalida",titulo:"NO FUE POSIBLE VALIDAR",detalle:error instanceof Error?error.message:"Error de validación"});}window.setTimeout(()=>{bloqueado.current=false;},1200);return;}
    setResultado({estado:"invalida",titulo:"CÓDIGO NO RECONOCIDO"});window.setTimeout(()=>{bloqueado.current=false;},1200);
  }
  const comprobarDesdeCamara=useEffectEvent(comprobar);
  useEffect(()=>{let lector:import("html5-qrcode").Html5Qrcode|null=null;let cancelado=false;void import("html5-qrcode").then(async({Html5Qrcode})=>{if(cancelado)return;lector=new Html5Qrcode(elementId);try{await lector.start({facingMode:"environment"},{fps:10,qrbox:{width:240,height:240},aspectRatio:1},texto=>comprobarDesdeCamara(texto),()=>undefined);}catch{setErrorCamara("No pudimos abrir la cámara. Usa el código o la búsqueda manual.");}});return()=>{cancelado=true;if(lector?.isScanning)void lector.stop().then(()=>lector?.clear()).catch(()=>undefined);else lector?.clear();};},[elementId]);
  function validarManual(e:FormEvent){e.preventDefault();if(codigo.trim())void comprobar(codigo);}
  async function consumirBeneficio(){if(!beneficioPendiente)return;const respuesta=await fetch("/api/access/validate-reward",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({code:beneficioPendiente,action:"consume"})});const remoto=await respuesta.json() as {result?:string;reward_name?:string;participant_name?:string;error?:string};setBeneficioPendiente("");setResultado(respuesta.ok&&remoto.result==="delivered"?{estado:"aceptada",titulo:"BENEFICIO APLICADO",nombre:remoto.participant_name,detalle:remoto.reward_name}:{estado:"invalida",titulo:"NO FUE POSIBLE APLICAR",detalle:remoto.error??remoto.result});}
  const texto=busqueda.trim().toLowerCase();
  const hallazgos:Hallazgo[]=texto?[...entradas.filter(e=>(!eventoId||e.eventoId===eventoId)&&[e.titular,e.codigo,e.email].some(x=>x.toLowerCase().includes(texto))).map(e=>({id:e.id,tipo:"entrada" as const,codigo:e.codigo,nombre:e.titular,detalle:`Entrada ${e.tipoNombre} · ${e.estado}`,eventoId:e.eventoId,habilitado:e.estado==="valida"})),...reservas.filter(r=>(!eventoId||r.eventoId===eventoId)&&[r.titular,r.codigo,r.telefono,r.email].some(x=>x.toLowerCase().includes(texto))).map(r=>({id:r.id,tipo:"reserva" as const,codigo:r.codigo,nombre:r.titular,detalle:`${r.tipoNombre} · ${r.estado}`,eventoId:r.eventoId,habilitado:r.estado==="confirmada"})),...listas.flatMap(l=>l.invitados.filter(i=>(!eventoId||l.eventoId===eventoId)&&[i.nombre,i.codigo,i.telefono,i.email].some(x=>x.toLowerCase().includes(texto))).map(i=>({id:i.id,tipo:"lista" as const,codigo:i.codigo,nombre:i.nombre,detalle:`Lista ${l.nombre} · grupo de ${1+i.acompanantes} · ${i.estado}`,eventoId:l.eventoId,habilitado:i.estado==="confirmado"})))].slice(0,20):[];
  const filtro=(id:string)=>!eventoId||id===eventoId;const entradasDentro=entradas.filter(e=>filtro(e.eventoId)&&e.estado==="usada").length;const reservasDentro=reservas.filter(r=>filtro(r.eventoId)&&r.estado==="usada").reduce((s,r)=>s+r.personas,0);const listasDentro=listas.filter(l=>filtro(l.eventoId)).flatMap(l=>l.invitados).reduce((s,i)=>s+i.ingresados,0);const aforo=entradasDentro+reservasDentro+listasDentro;
  const estilo=resultado?.estado==="aceptada"?"border-lime bg-lime/10 text-lime":resultado?.estado==="usada"?"border-amber bg-amber/10 text-amber":"border-danger bg-danger/10 text-danger";
  return <main className="flex-1 px-4 py-5 max-w-3xl mx-auto w-full space-y-5"><header className="flex justify-between gap-4"><div><Logo size="text-2xl"/><h1 className="font-bold text-lg mt-1">Acceso unificado</h1></div><Link href="/accesos" className="text-sm text-muted">← Roles</Link></header><section className="grid grid-cols-[1fr_120px] gap-3"><select value={eventoId} onChange={e=>setEventoId(e.target.value)} className="card px-4 py-3 bg-background"><option value="">Todos los eventos</option>{EVENTOS.map(e=><option key={e.id} value={e.id}>{e.nombre}</option>)}</select><div className="card p-3 text-center"><p className="text-xs text-muted">Aforo actual</p><p className="text-2xl font-bold text-neon3">{aforo}</p></div></section><div className="grid md:grid-cols-2 gap-5"><section className="space-y-3"><div id={elementId} className="rounded-2xl overflow-hidden bg-black min-h-72 border border-line"/>{errorCamara&&<p className="text-xs text-amber text-center">{errorCamara}</p>}<form onSubmit={validarManual} className="flex gap-2"><input value={codigo} onChange={e=>setCodigo(e.target.value)} placeholder="Entrada, reserva, lista o beneficio" className="card flex-1 min-w-0 px-4 py-3 bg-transparent outline-none font-mono text-sm"/><button className="rounded-xl px-4 border border-neon3/50 text-neon3 font-semibold">Validar</button></form>{resultado&&<section className={`rounded-2xl border-2 p-5 text-center ${estilo}`}><p className="text-4xl">{resultado.estado==="aceptada"?"✓":resultado.estado==="usada"?"!":"×"}</p><h2 className="text-xl font-black mt-2">{resultado.titulo}</h2>{resultado.nombre&&<p className="font-semibold mt-2">{resultado.nombre}</p>}{resultado.detalle&&<p className="text-sm opacity-80">{resultado.detalle}</p>}{beneficioPendiente&&<button onClick={consumirBeneficio} className="mt-4 w-full rounded-xl bg-lime text-black p-3 font-bold">Confirmar y aplicar beneficio</button>}</section>}</section><section className="space-y-3"><div><h2 className="font-bold">Búsqueda manual</h2><p className="text-xs text-muted">Nombre, teléfono, correo o código.</p></div><input value={busqueda} onChange={e=>setBusqueda(e.target.value)} className="entrada" placeholder="Buscar persona"/>{hallazgos.map(h=><article key={`${h.tipo}-${h.id}`} className="card p-4"><div className="flex justify-between gap-3"><span><span className="text-[10px] uppercase text-neon3">{h.tipo} · {eventoNombre(h.eventoId)}</span><b className="block">{h.nombre}</b><span className="text-xs text-muted">{h.detalle}</span></span><button disabled={!h.habilitado} onClick={()=>comprobar(h.codigo)} className="btn-neon rounded-xl px-3 text-sm disabled:opacity-40">Ingresar</button></div></article>)}{texto&&!hallazgos.length&&<div className="card p-6 text-center text-muted">Sin resultados.</div>}<section className="card p-4 grid grid-cols-3 gap-2 text-center"><Dato t="Entradas" v={entradasDentro}/><Dato t="Reservas" v={reservasDentro}/><Dato t="Listas" v={listasDentro}/></section></section></div><p className="text-xs text-muted text-center">Validar un beneficio no lo consume; requiere confirmación explícita del comercio.</p></main>;
}
function Dato({t,v}:{t:string;v:number}){return <div><p className="text-[10px] text-muted">{t}</p><p className="font-bold">{v}</p></div>}
